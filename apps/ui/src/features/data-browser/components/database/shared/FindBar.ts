import { FindBarBar } from "./FindBar.Bar";
import { FindBarProvider } from "./FindBar.Provider";

export const FindBar = {
  Bar: FindBarBar,
  Provider: FindBarProvider,
};

export { useFindBar } from "./FindBar.Provider";
